export const VIMEO_HOSTNAME = "api.vimeo.com";

export const AUTH_ENDPOINTS = {
  authorization: "/oauth/authorize",
  accessToken: "/oauth/access_token",
  clientCredentials: "/oauth/authorize/client",
};
